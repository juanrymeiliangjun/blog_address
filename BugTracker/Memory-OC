## Leaks

### Kapi-Actions

##### 关于内存泄漏的查找及解决方案

1. 创建的对象未释放，导致切换界面后不断增加内存

   **查找工具**：Xcode16(Debug Memory Graph)

   **查找方法：**切换页面后检查上一个页面的对象类型搜索

   **解决方法：**重点查看Block引用的对象，包括传入的对象，本身的对象，尽量使用weakSelf来引用对象内部的数据。防止循环引用

2. 类中声明的强引用对象，需要及时主动的置为空，否则一直被引用
